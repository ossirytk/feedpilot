{
    "mcpServers": {
        "feedpilot": {
            "command": "uv",
            "args": [
                "run",
                "feedpilot"
            ],
            "cwd": "${workspaceFolder}",
            "description": "Daily tech digest MCP server for RSS/Atom feeds"
        }
    }
}